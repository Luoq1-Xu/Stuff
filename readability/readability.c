#include <cs50.h>
#include <stdio.h>

int main(void)
{
   string text = get_string("Text: \n");
}




int count_letters(string text)
{
    for (int i=0;text[i] != '\0'; i++)
    {
        if (((text[i] >= 65) && (text[i] <= )))

    }
}